# 徕卡经典暗角着色器

在相机设置的经典风格控制区域打开“徕卡经典暗角着色器”。

- **暗角强度**：0–400%，默认 100%。0% 旁路这个着色器的压暗函数。
- **高光保护**：默认开启，保留原函数随像素亮度减弱压暗的行为。
- **暗角形态调节**：独立卡片，包含范围与形状两个控件。
- **暗角范围**：0–150%，默认 100%，以 1% 为步长。0% 不施加暗角；100% 保留原范围；增大数值使暗角更靠近画面中心，减小数值使暗角更靠近边缘。
- **暗角形状调整**：默认开启，保留原生的横向 0.75 倍坐标压缩；关闭后取消固定横向压缩，按归一化画面坐标的径向距离计算。
- **实验性**：导入 UTF-8 `.glsl` 函数体，最大 16 KiB。导入时执行 GLES 编译与链接检查；导入成功后再手动启用。
- 启用自定义着色器后，普通强度、高光保护、范围和形状控件停用；关闭自定义着色器后恢复之前的普通设置。

“柔光控制组件”位于相机设置的“实验室选项”，默认开启，已有选择会保留。

文件内容是 `colorDark(color, uv, darkStrength)` 的函数体，可使用 `color`、`uv`、`darkStrength`、原有 `PREFIX(...)` uniform 和 `INPUT(...)` 采样，返回 `vec4`。不要再包裹完整函数声明。例如：

```glsl
return color;
```

更换公式后关闭并重开相机，以加载新的着色器；无需为每次公式修改重新刷包或重启手机。

## 安装和挂载

首次接入须安装 All-in-One 整包并按安装器提示重启。单独安装 LSP APK 不会安装成片读取器与公式同步服务。

本版按同一套固定库方案提供给不同机型测试，不设置机型限制：

- 相机 APK 的 `libMiFilterSDK.so` 接入预览公式读取。
- 模块的 `payload/libMiPhotoFilter.so` **只 bind mount 到 `/odm/lib64/libMiPhotoFilter.so` 这一个文件**。
- 不挂载整个 `/odm` 或 `/odm/lib64`。已有视频滤镜、水印功能继续只合并各自的配置子目录。
- KernelSU、Magisk、APatch 均走上述挂载范围，公式监听服务使用对应环境可用的 BusyBox。

成片库以已核对的小米 14 Ultra 原库为基础构建。不同机型上的成片兼容性仍需要实机测试；管理器脚本兼容不等于所有机型的库兼容都已验证。

如果仍启用了此前的 `Phoenix-Dev1` 独立模块，请在使用正式整包前停用它，避免两个模块覆盖同一个照片库。正式包会迁移现有 Dev1 的公式与已导入文件，保留相机内原有设置。

## 维护接口

同步文件位于：

- `/data/user/0/com.android.camera/files/phoenix-vignette/colorDark.glsl`
- `/data/vendor/camera/phoenix-vignette/colorDark.glsl`

相机设置通过 request/ack 协议提交修改，根模块负责同步两份文件并保留各自 owner 与 SELinux 标签。新安装初始化为原厂公式；更新安装保留已有公式。

此入口只控制经典后处理的 `colorDark`，不调整 LUT、LSC、曝光、HDR 或其它成像节点。

范围在计算原生暗角曲线前，将中心距离乘以 `范围 / 100`；强度继续控制亮度衰减指数。范围为 0 时直接返回原像素。形状开启、范围 100% 时生成的函数体与旧版相同，不改变原生默认效果。形状关闭只取消固定坐标压缩，不构成镜头光学标定模型。
