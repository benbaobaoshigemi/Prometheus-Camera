Phoenix-1.0.7
- 新增暗角形态调节卡片：范围 0–150%（默认 100%）与默认开启的原生形状调整开关。
- 实验室选项提供柔光控制组件开关，默认开启；暗角设置页只提供暗角控制。
- 保留 1.0.6 的关闭日志修复；自定义着色器关闭后恢复之前的强度、保护、范围和形状。

Phoenix-1.0.6
- 修复关闭 Phoenix 日志后，相册编辑器启动阶段仍向 Download 写入诊断日志的问题。

Phoenix-1.0.5
- 新增徕卡经典暗角着色器设置：0–400% 强度、高光保护、自定义 GLSL 导入与启用。
- 相机预览与成片公式同步；更新保留已有公式，并迁移独立开发模块的设置文件。
- 成片读取器仅挂载 /odm/lib64/libMiPhotoFilter.so 单文件，不挂载整颗 ODM 或整个 lib64 目录。
- 挂载与公式监听兼容 KernelSU、Magisk、APatch 脚本环境，不设置机型限制。
- 保留 1.0.4 相机启动修复；安装自检精确核对活动 APK 的版本号。

Phoenix-0.6.25-test1
- 控制变量包：模块内容回归 Phoenix-0.3.0；Camera 与 LSP APK 保持原字节和原版本。
- 模块标识单独递增，安装自检明确校验两个 APK 仍为 Phoenix-0.3.0。

Phoenix-0.3.0
- 基于 NRV-1.5.0 根模块完整重打包；保留原刷入、诊断、管理器兼容、水印事务与挂载链路。
- 更新 Phoenix Camera/LSP 与业务资产，取消刷入阶段机型校验。

NRV-1.5.0
- 基于当前已验证稳定版统一版本号为 1.5.0（相机 750005500 / LSP 与模块 1005000）。
- 夜景 EV 与滤镜均使用 17U 原生控件；补齐后置夜景滤镜标签初始化，并修正快门模式补丁未实际写入的问题。
- 恢复夜景自定义快门；EV 使用夜景原生曝光调节链；滤镜仅解除夜景原生门禁并保留完整初始化；修正链检查可选状态误报与汇总重复计数。
- 解限夜景模式底部 EV 调节与 Leica 原生滤镜入口，沿用当前底部 action 管线，不引入 14U 控件。
- 刷入时直接写入 33 个内置预设滤镜，重启后相册编辑器立即可用。
- 新增 action.sh 链检查：核对相机/相册编辑器/滤镜/水印文件的完整性与注入情况，前台简洁报告，完整报告保存到模块目录。
- 优化刷入文案：统一为 Prometheus Cam，移除黑话与冗余说明，安装异常时提示“请检查核心破解”，完成后三次提示直接重启。
- 构建器重做：以 fixed-3 为唯一基线，构建后逐项核对，确保不混入实验内容。

NRV-2.0.1
- 真机确认14U经典成片执行CvEffect而非CvStyleEffect；撤销无效的17U libMiPhotoFilter.so替换，恢复14U ROM原生效果引擎。
- 静态门禁要求14U/17U CvEffect GLSL逐字一致，并禁止独立MiPhotoFilter载荷或bind逻辑回流。
- 保留柔焦level 0修复；不引入字段翻译、效果注入或REV式投影。

NRV-2.0.0
- 修复柔焦着色器读取不存在 mip level 4 导致效果不可见的问题，沿用显式降采样并读取 level 0。
- 为柔焦shader初始化和CvStyle LUT/Shading成对挂载增加低频管线日志点。
- 以YUV420_NV12为边界固定14U原生RAW/ISP闭包，禁止17U核心Bayer/MFNR/tuning资产误入。
- 将经典/生动逆向报告与当前NRV真实闭包交叉校正，未验证的gamma和CvStyle门控结论不再作为实现依据。

NRV-1.0.27
- 恢复“17 Ultra by Leica”动态预设并修复普通预设点击崩溃。
- by LEICA 预览直接解码已提交的完整拼接资产，不再显示透明占位。
- 相册编辑重建对齐 Camera 的通用 Ti 产品字段分段规则。
- 修复清数据首次启动时预置 LUT 目录的跨进程创建竞态。

NRV-1.0.24
- 相册目录保留原生设备、日期、区域和照片条件过滤，只补充 NRV 本地专属的签名 24–26 与色卡 27–30。
- 撤销会改写相册机型名并破坏 Xiaomi 预设资产的实验性重建钩子；不再调用不存在的位图加载方法。
- 专业模式顶栏定时连拍复用 Leica-SP 原生开/关 Lottie 资源和真实状态，不再强塞静态图标。
- 保留 V5.1 水印路由器的完整 MIVI 后处理 ABI，只移除固定 by LEICA 机型分支。
- 相册重建按颜色文件名直接读取已校验的 by LEICA 整图事务，不再二次渲染或把整图塞入子资产层。
- 整图事务命中后跳过模板自带的 model/model_versionName 文字元素，避免完整 Logo 后再次追加机型文字。
- 滤镜滑动热路径不再重复提交未变化的本地镜像；颗粒/柔光合并为一次统一表读取，修复卡顿后崩溃。

NRV-1.0.16
- 修复专业模式定时连拍移入顶部后可点击但图标不可见：原生 holder 现在消费原生图标资源，不改变视图类型与编辑布局。
- 修复 Leica 111 错打包整句 17 Ultra 素材造成的机型名叠加，恢复品牌层、机型层、后缀层的单一职责。
- by LEICA 预览路由同时依据持久化选择与解析名称，避免解析拆分后错误回退为仅 xiaomi。

NRV-1.0.15
- 修复动态机型名未进入原生 by LEICA 素材路径而回退显示 xiaomi；完整机型名仍由在线渲染缓存提供。
- 修复专业模式定时连拍组件被错误强制转换为 StrikethroughImageView 的崩溃，不引入视图重定向兼容桥。
- 修正 Leica 111 水印的打包来源，移除 alpha50 遗留的 lcc 机型限制，使黑底黄字模板进入菜单。

NRV-1.0.14
- 补齐黑底黄字 Leica 111 水印及其他配置已引用但缺失的背景选择图标，注册资产引用闭合。
- 将人像模式纳入V5.1拍照类模式判定，使整套自定义快门样式业务在人像模式生效。
- 撤销非V5.1的TimerBurst视图类型重定向，恢复顶栏编辑器真实组件与位置动画配对。

NRV-1.0.13
- 修复干净安装时 root 提前创建应用 files 目录导致相机无法创建滤镜事务目录的问题。
- 安装与开机初始化会校正相机/相册私有 files 目录及既有 prometheus 事务目录属主，不清除应用数据。

NRV-1.0.12
- 删除新增的 ODM prometheus 业务资产子树，Root 模块恢复只承担 V5.1 原有职责。
- 33 个预设滤镜和 24 个经典风格资产改由 LSP APK 持有，不再依赖 ODM 投影。
- 相机首次加载目录前完成预设资产哈希校验、原子落盘和事务提交；相册同步只消费提交事件。

NRV-1.0.11
- 修复 KernelSU 管理器手动刷入并立即重启后，LSPosed 启用状态和作用域被旧备份恢复的问题。
- 不再在刷入阶段直接修改 LSPosed 数据库；改为 LSPosed 启动后执行一次中文可审计事务。
- 不清除应用数据，不检测版本，不循环补装 APK，不使用定时器反复重写作用域。

NRV-1.0.10
- 修复手动刷入后 LSPosed 异步更新清除模块启用/作用域，导致相机缺少业务 DEX 而崩溃。
- 安装期等待 LSPosed 确认新 APK 路径，并持续验证启用和三端作用域稳定后才完成。

NRV-1.0.9
- 恢复 V5.1 动态 by LEICA 路由，完整机型字符串在相机运行时渲染。
- 携带并校验桌面版 MiSans Latin VF；移除 14 Ultra 静态颜色变体枚举。

NRV-1.0.8
- 修复原子提交触发的目录 moved-in 事件未被消费，恢复用户滤镜同步到相册
- 将新增水印缓存与徕卡后缀资产同步到 MediaEditor

NRV-1.0.7
- 恢复 V5.1 安装阶段的 ODM 目录整理，确保 post-fs-data 挂载输入完整
- 回到 V5.1 稳定 Root/KSU 模块架构和模块 ID
- 保留无 classes9 的 Camera/LSP 解耦架构
- 保留专业模式、手柄、经典风格、始终快门声和相册 HDR 修复
- 注册 33 个 preset LUT 与新版水印资产
- Camera 主库 CRUD 提交后事件驱动同步相册
