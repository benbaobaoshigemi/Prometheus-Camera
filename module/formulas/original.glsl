 vec3 factor = vec3(0.2125, 0.7154, 0.0721); float Y = dot(vec3(color), factor); uv.x = (uv.x - 0.5) * 0.75 + 0.5; float dist = distance(uv, vec2(0.5,0.5)); float pow = 1.0 / pow(100.0, (1.0 - Y)); float smoothStartValue = PREFIX(SmoothStartValue); float smoothEndValue = PREFIX(SmoothEndValue); float falloff = PREFIX(Falloff); return (color * (1.0 - pow) * smoothstep(smoothStartValue, falloff * smoothEndValue, dist * darkStrength) + color * pow); 